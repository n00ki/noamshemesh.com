const config = {
	content: ['./src/**/*.{html,js,svelte,ts}'],
	theme: {
		extend: {
			fontFamily: {
				poppins: ['poppins', 'sans-serif']
			}
		}
	},
	darkMode: 'class',
	plugins: []
};

module.exports = config;
